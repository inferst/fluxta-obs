# Fluxta OBS

A Fluxta plugin that controls OBS Studio over its own WebSocket protocol
(via `obs-websocket-js`): switch Scenes, start/stop/pause the Stream and the
Recording, show/hide/toggle a Source, mute/unmute or set an Input's volume,
and enable/disable/toggle a Filter — plus the matching Events and Value
Sources for scenario builders.
